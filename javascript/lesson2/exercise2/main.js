/* Il doit compter de 0 à 100. (afficher le nombre à chacune des itérations).*/
//for ( var compteur = 0 ; compteur <= 100 ; compteur++ ) {
//    console.log("Je compte jusqu'à " + compteur);
}
/* Maintenant, transformez le pour qu'il compte de deux en deux.*/
//for(var compteur = 0 ; icompteur<= 100 ; compteur = compteur + 2);{
//    console.log( "je compte de deux en deux")
}
/* Inversez le pour qu'il décompte de 2 en 2 de 100 à 0.*/
for(var compteur = 100 ; compteur >= 0 ; compteur= compteur - 2);{
    console.log( "je décompte de deux en deux")
}// (ou correction) for(var compteur= 100; comptur>=0; compteur -=2);